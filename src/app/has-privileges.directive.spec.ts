import { HasPrivilegesDirective } from './has-privileges.directive';

describe('HasPrivilegesDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPrivilegesDirective();
    expect(directive).toBeTruthy();
  });
});
